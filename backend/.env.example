# MongoDB connection string
# Get your MongoDB URI from MongoDB Atlas or your MongoDB instance
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/phonebook?retryWrites=true&w=majority

# Test database URI (optional, if omitted tests fallback to MONGODB_URI)
TEST_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/phonebook_test?retryWrites=true&w=majority

# Server port (default: 3001)
PORT=3001
